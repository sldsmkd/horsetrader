# Eclipse 2 — The Design

_The settled model, stated cold. Derivation and superseded alternatives live in 1.NORMALISATION.md; this is what gets built._

**The loop:** shields write account state; streams read it into events; the fold reads events into money; surfaces read both; repeat. Nothing else moves.

## Currency
- **Streams produce EVENTS, not rewards.** Rewards are a property of events; money is a fold over them; cards are a view over them. One currency, two projections — the ledger and the lane.

## Stream
- `Stream = id + enabled(ctx) + claims + events(ctx) → list[Event]`. One verb.
- `id` — dotted `selector.name` (`play.routine`, `ground.events`, `subscription.daily-pack`). Names the stream; never names events. The old `flow` axis is dropped: every stream is income by construction (balance-readers are reconciliation rules, never streams), so `income.` carried zero bits and `spend.*` has no occupants — spends aren't streams.
- `enabled(ctx)` — the on/off gate, owned by the stream. Whole-stream gating is safe by commutativity.
- How a stream makes its events is private: **claimers** price the baked events they own (stamp a play-graded row, pass through, filter); **synthesisers** mint events the bake doesn't carry (`routine-dailies-<date>`, `visible: false` — on the ledger, off the lane).
- Streams are mutually anonymous. No stream knows another exists; no ordering between them.
- Shared rule `GradedStamp` (N=2: champions-meeting, stories; PvP trio #61 next): return own claimed events priced by the play-selected `reward_maps` row; `row(ctx, event)` is the sole varying part.

## Partition
- `claims` = event **types** (`("cm",)`, `("story",)`) — AMENDED 2026-06-12 by 4.REGISTRY derivation 1: the census shows prefix is coarser than type where it matters (`mission-` spans three types across the play gate), and aligning prefix=type via ETL would only bake a redundant copy of the type field. Types are the bake's own disjoint taxonomy (one per record, msgspec-guaranteed).
- Disjointness asserted at registry construction — double-pay is unconstructible. `ground.events` claims the complement (of claimed types).
- Uniqueness within a claim is the bake's guarantee (msgspec map): trust the bake; a collision is a bug.
- Emission `source` = the event's stable key. Minted keys give sub-source attribution for free; one naming system. Synthesisers' minted prefixes are fenced at construction (disjoint from baked first tokens and each other) — prefix discipline routes nothing; it fences minting.
- **An owner settles ALL of its events' facets** (4.REGISTRY derivation 2): compound shapes (`generator`/`sequence`) are expanded by the owning stream into `visible:false` cadence children minted under the parent key — stream-private business logic (a shared rule beside `GradedStamp`), never separate streams.

## Coordinator
- assert partition → **union = the settled world** → one generic fold → `P_income` → reconciliation → `P_final`.
- The settled world is simultaneously the render source (cards = its visible events, reward faces included) and the fold input. "What am I rendering?" is answered by construction.
- Money derivation exists ONCE, in the fold: pay at `end`, `start` when rushed, `flatPayload` the rewards. Payment timing is unwritable in streams.
- **The fold is linear**: `balance = base + Σ deltas`. No clamping, no min/max, no balance-conditionals — negative free carats are allowed and meaningful. Linearity + disjoint claims ⇒ streams commute; registry order means nothing; per-stream dirty/refold is legal (unbuilt — full regenerate ships).
- The coordinator holds the settled world and both boundary projections. It has no ordering responsibility and never introspects a stream.

## Reconciliation
- There is no late *stream* — `LateStream` is dissolved. Reconciliation is **coordinator machinery**, the third pipeline stage: `union → fold → P_income → reconcile(commitments) → P_final`. The wall survives as pipeline ordering (P_income must exist before reconciliation reads it), not as a type. Streams produce events, full stop; balance-readers are reconciliation rules, never streams.
- **Banners are just events; they have no spends.** They flow through the partition, render, and would fold rewards like anything else. A commitment is account state the USER raises *against* an event (a stable-key reference: `commitments[key] = claim`); the referent never knows. Any event can carry a claim — in practice it's banners (a fact about which cards grow a shield, not about the model). Three layers: event, claim (account state), debit (derived emission).
- Reconciliation's domain is `ctx.commitments`: iterate the claims, dereference each referent in the settled world (resolve-or-throw), use it only for dates, emit the debit. Loop over claims dereferencing events — never over events checking for claims.
- **Debit `source` = the claimed event's stable key** (`banner-kita` cost you X — readable straight off the ledger; one naming system, no parallel spend vocabulary). Provenance is positional, not nominal: debits exist only in P_final's reconciliation tail, so income-vs-spend is which projection you read, never a string on the row.
- **Ordering: (start date, stable key) — deterministic, and it only affects attribution.** Debits commute (linear fold), so P_final is ordering-independent; reconciliation order matters solely for claim *seniority* in availability queries. Start-ordering follows the earmark: money is put aside as soon as the bill is present (debit folds at `start`), paid when due (measured at `end`) — seniority binds at earmark, not at maturity. (Matches the shipped pass.)
- `available` = coordinator query against `P_income` (not `P_final`), re-running the pure ordered reconciliation on demand, self-excluding by construction: "what would a claim against me see?"

## Read / write
- **The projection is never written — only derived.** Account state (snapshot, commitments, play, identity, subscriptions) is the write store; the fold is a pure query over `(bake, account state)`; ctx is frozen for a fold's duration.
- One write path: shields are transactional editors over account-state fields (commit pity ≡ change a play slider — same operation, different field). COMMIT → mark dirty → re-derive → refresh views.
- A commitment is a stored claim, read fresh by every fold; a fully-spent past dissolves into the snapshot base. Nothing needs reversing because nothing was written: re-derivability, not reversibility.
- Bake immutable; the settled world lives one fold; never derive from a derived world.

## Prerequisite — SATISFIED
- #62 (anchor retirement) landed 2026-06-12: `before-`/`after-` keys deleted end-to-end — the one key family whose *first token was a relation, not a type* is gone. (The invariant this bought was originally stated as "every prefix routes to exactly one stream"; the claims unit has since moved to types — see Partition — but #62 remains load-bearing: an anchored event's type/key carried no routable identity at all.) The partition model is buildable as written.
