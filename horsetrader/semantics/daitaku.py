def daitaku(cls):
    """Daitaku — the *when*.

    Daitaku Helios is the party girl whose phone is buried under
    notifications — never missing an invite, always knows what's
    happening, where, and when. Her module owns the event calendar:
    when banners run, when events drop, when campaigns are on, JST↔UTC
    handling (CLAUDE.md §3 "Game day = JST"), and any other "when does
    this happen / what's on right now" lookup.

    If code is answering a date/period/calendar question, it's
    Daitaku's.

    Character bio: see ``daitaku.md`` alongside this file.
    """
    return cls
