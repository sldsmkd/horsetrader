def shuttle(cls):
    """Shuttle — Japlish and translations.

    Taiki Shuttle is the U.S.-born cowgirl who peppers her Japanese with
    English ("BANG! ☆"), the most naturally bilingual of the cast. Her
    module owns the language seam: mapping between JP source strings and
    their EN projections — name/alias translation, romanisation, locale
    selection, and the curated overrides where the machine projection
    isn't good enough. When code has to decide *what something is called
    in the other language*, Shuttle owns it.

    She does **not** own knowing *what* an entity is (Digitan) or *when*
    it happens (Daitaku) — only how its words cross the JP↔EN boundary.

    Character bio: see ``shuttle.md`` alongside this file.
    """
    return cls
