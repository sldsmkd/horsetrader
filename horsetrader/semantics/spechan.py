def spechan(cls):
    """Spechan — logging (cross-cutting).

    Special Week's frequent purchase is letter sets — she's always
    writing detailed updates home to her moms — and she also likes
    "detailed food reports." Her module owns logging across the
    pipeline: structured records of what happened, dispatched somewhere
    to be read later. Other modules call into her when they want to
    write a log line; she owns formatting/levels/sinks.

    If code is "tell someone what just happened" without affecting the
    pipeline output itself, it's Spechan's.

    Character bio: see ``spechan.md`` alongside this file.
    """
    return cls
