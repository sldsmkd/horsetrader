- [Docs are the source of truth](project_docs_pointer.md) — `docs/` documents both sides now: `etl/`, `frontend/`, crosscutting `contract.md` (etl↔site bundle) + `domain.md`, and `references/`. Read before answering codebase questions.
- [Prediction subsystem complete](project_prediction_complete.md) — as of 2026-05-30 the predictor chain is closed end-to-end (nothing hits `unpredicted`); DateMapper + FallthroughPredictor are the catch-all. Remaining ETL frontier is data sources.
- [Projection engine partially built](project_projection_engine.md) — frontend pillar 2: fold + ledger + events/generator channels done & tested; spends/commitments PARKED but ETL blocker now cleared (sequence type shipped). See docs/frontend/projection.md status before resuming.
- [UI view layer captured intent-first — COMPLETE](project_ui_design_doc.md) — `ui/` design distilled surface-by-surface into docs/frontend/ui.md; as of 2026-06-02 ALL surfaces captured (spine, timeline, banner, bookmarks, menubar, search, account, dev panel, what's-new, planner, edge cases). Doc feature-complete; next is `ui/` code. Only open *threads* remain inline.
- [Account UI mirrors Henry Handsome calculator](project_henry_handsome_prior_art.md) — the account/config model copies the community's de-facto planning spreadsheet so concepts are pre-taught; "edge over a spreadsheet" made literal.
- [SequenceReward type shipped](project_sequence_reward_type.md) — ETL ships the daily-login sequence value type (single type, nullable=absence); the client owns the strategy collection/overlay/affordability. Not listable ETL-side (N=1).
- [JP is the substrate, EN is projected](feedback_jp_is_substrate.md) — build the JP scrape first; never build from the EN YAML (that inverts the model). Agents get lost here.
- [Data sources: official EN site + Gametora / GameWith / wikiru](reference_data_sources.md) — umamusume.com/news = first-party EN truth (official names, exact UTC, payloads) for verify/overlay; Gametora=database (live primary); GameWith=structured rewards esp. anniversaries; wikiru=community event index. Only Gametora ingested live; JP wikis are one-off bootstrap→curated YAML; EN site is projected-truth not a substrate.
- [Showtime is a closed 2-event series (curate, don't predict)](project_showtime_closed_series.md) — フジキセキのショータイム = exactly 2, Fuji-Kiseki-only; EN shipped the final one Jun 4–14 2026. Finite curated data, not a predictor; still un-curated.
- [League of Heroes is unmodelled (TODO)](project_league_of_heroes_todo.md) — quarterly PvP event that replaces a CM (Feb/May/Aug/Nov from 2025); no data yet, revisit CM predictor when it lands near Global's cm-025 transition.
- [Monorepo consolidation + boundary discipline](project_workspace_split.md) — one repo at `/home/kris/code/horsetrader/` (ETL = root, `horsetrader.site/` = tracked frontend subtree after its sub-repo was deleted 2026-06-01); `static/`/`.cache/`/`_horsetrader.old/`/`node_modules` gitignored. Sessions are single-purpose: crossing the backend↔frontend line = stop and warn loudly (workflow discipline, not a git boundary). No cross-side handoff file — `generated/` was removed 2026-06-01.
- [Full build pipeline BUILT (root Makefile)](project_build_pipeline.md) — `make` runs seed(skeleton→static) → bake → gen:types → build; `make dev`/`serve` = esbuild live server; `make deploy` STUBBED (live prototype, don't re-enable wrangler); `make clean` = only teardown. `package*.json` stays in `horsetrader.site/` (don't re-litigate). Shippable artifact = gitignored root `static/` (`json/`+`img/` from ETL via `Config.static`, `index.html`+`js/` from build, skeleton shell overlaid first); schema → `config/schema/`. `generated/` removed entirely. `HORSETRADER_TARGET`/`_target()` still dead code.
- [Site is the front-end concern at `horsetrader.site/`](project_site_consumer_workspace.md) — zero-server static TS planner, a tracked subtree of the monorepo; docs now in `docs/frontend/`; consumes the bake, trusts it raw. Don't edit from a backend session.
- [Output-breaking allowed (until FE rebuild ships)](project_output_breaking_allowed.md) — as of 2026-05-30 free to change serialized output shape; prototype is throwaway, constraint returns when new front end is in production.
- [Explicit-contract overrides are load-bearing](feedback_explicit_contracts.md) — concrete subclasses must declare trivial `super()` overrides; never flag as dead code (per docs/standards.md).
- [Design at principle altitude first](feedback_design_altitude.md) — when designing infra/architecture, stay on general principles/mechanisms; don't prematurely dive into the specific data model or field-level "what". (Merged from the site brain.)
- [Marker classes via primitive inheritance](feedback_marker_classes_via_primitives.md) — user's instinct is `class X(Marker, int): pass`; offer composition (`amount: int` field) when MI brings real cost.
- [Allowlist drops are debug, not warning](feedback_allowlist_drops_are_debug.md) — typed-subset filtering drops the long tail by design; warning is for surprise. "Unmapped" reads better than "unknown".
- [Static yaml region-namespace pattern + keystore](project_static_yaml_region_namespace.md) — merged static yamls use `jp:`/`en:` as namespaces (not scalars); all 5 corpora done. Store globs every `*.yaml` in `config/yaml/` into one key-indexed corpus; loaders select rows by `_KEY_PATTERN` via `store.select()` (filenames cosmetic, entries can live in any file); provenance via generic `store.source()`. Anchored events inline by `before-`/`during-`/`after-` prefix.
- [`en.name` is just the EN string (data quality, not code)](project_scenario_name_precedence.md) — software doesn't care fansub vs official; `en.name` preferred over `jp.name` for display, `en.start` marks "shipped". Don't model a fansub/official distinction in loaders. Stories are the one real cross-source override (curation beats scrape).
- [FQ timestamps in curated YAML](feedback_fq_timestamps_in_yaml.md) — store `start: 2022-01-01T05:00:00+09:00` in the YAML, never `start: 2022-01-01` + a hidden `_drop_hour()` in the loader.
- [Structure over comments in curated YAML](feedback_yaml_structure_over_comments.md) — load-bearing blocks beat comment-annotated lists; structure is guaranteed, comments aren't, and tz offsets are easy to lose.
- [Curated YAML fails loud, not warn-and-skip](feedback_curated_yaml_fails_loud.md) — hand-curated `config/*.yaml` should `raise` on validation failure; the pipeline run is the editor's IDE feedback loop. Scraped extractors stay warn-and-skip.
- [Git workflow: branch frontend, main for ETL](feedback_git_workflow_phase.md) — as of 2026-06-03 frontend `ui/` features go branch-per-feature (the end-to-end loop is proven; e.g. `4a-xfordate`); ETL/pre-baseline work still commits straight to `main`, no ceremony. Test: does a proven path exist here this could break?
- [Use the real cache for verification](feedback_use_real_cache_for_verification.md) — use the warm shared cache, don't blow it away; since 2026-06-01 it lives at the repo root `.cache/`, decoupled from `HORSETRADER_TARGET` (which now only sets the output root).
- [YAML keys must match stable keys](feedback_yaml_keys_match_stable_keys.md) — curated `config/*.yaml` keys are byte-for-byte stable keys; no upstream formats, no zero-pad normalization in the loader.
- [Work from the back when extracting shared layers](feedback_work_from_the_back.md) — at N=2 ship the smallest primitive that removes literal duplication; let further generalisations lift themselves up as more concrete cases land.
- [Explicit beats trivial DRY](feedback_explicit_over_trivial_dry.md) — a one-line guard (e.g. `if (date <= after) continue;`) stays inline & duplicated even at N=3; don't offer to extract a tiny shared predicate. House style is explicit.
- [Copy-a-neighbour keys for rarely-edited curated YAML](feedback_curated_keys_copy_a_neighbour.md) — for files touched a few times a year, load-bearing self-evident key prefixes + a copyable example beat an explicit field that needs docs/code to read.
- [Reuse the standing dev server](feedback_reuse_dev_server.md) — don't stop/start a server for browser checks; probe :3000, ask once if down, reuse it all session (drive chromium via CDP against it).
- [Projection is account, not view](feedback_projection_is_account_not_view.md) — core/projection = facts about your account only; presentation settings (axis scale/xForDate, zoom, future theme) live in ui/, same class as dark mode. Persisted prefs are a third bucket, still not in the plan.
